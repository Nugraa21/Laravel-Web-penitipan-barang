php artisan config:clear
php artisan cache:clear
php artisan view:clear
php artisan route:clear

# Kalau pakai Vite (Laravel 9+ default)
npm run build   # atau yarn build / pnpm build

# Kalau masih development & mau hot reload (opsional)
# npm run dev